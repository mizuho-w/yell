<div class="row">
	<div class="carousel slide" id="c1" date-ride="carousel">
		
	</div>

	<div class="col-8">
		<h3>人気のアイテム</h3>
		<div class="">
			<% @items.each do |item| %>
			<div class="">
				<%= link_to item_path(item) do %>
				<%= attachment_image_tag(item, :img, :fill,100,100,fallback:"noimg.jpg",size:"100x100") %>
				<% end %>
			</div>
			<% end %>
		<div class="">
			<% @items.each do |item| %>
			<div class="">
				<%= link_to item_path(item) do %>
				<%= attachment_image_tag(item, :img, :fill,100,100,fallback:"noimg.jpg",size:"100x100") %>
				<% end %>
			</div>
			<% end %>
		</div>
		</div>
	</div>

	<div class="col-4"></div>
</div>
