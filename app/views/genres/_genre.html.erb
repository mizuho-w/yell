<table class="table-hover">
  <thead>
    <tr>
      <th>ジャンル検索</th>
    </tr>
   </thead>
   <tbody>
      <% @genres.each do |genre| %>
      <tr>
        <td><%= link_to genre.name, search_path(genre_search:genre.id) %></td>
      <% end %>
      </tr>
   </tbody>
</table>
